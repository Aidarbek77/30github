<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Biography and Achievements</title>
    <style>
        /* Base Styles */
        html {
            scroll-padding-top: 70px;
            scroll-behavior: smooth;
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            background-color: black;
            color: white;
        }

        /* Top Navigation */
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px;
            background-color: rgba(30, 30, 30, 0.9);
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            transition: background-color 0.3s ease;
            backdrop-filter: blur(10px);
        }

        .top-bar:hover {
            background-color: rgba(50, 50, 50, 0.9);
        }

        /* Search Bar */
        .search-bar input {
            padding: 8px 12px;
            border: 1px solid #3498db;
            border-radius: 20px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            transition: width 0.3s ease;
        }

        .search-bar input:focus {
            outline: none;
            width: 250px;
            box-shadow: 0 0 8px rgba(52, 152, 219, 0.6);
        }

        /* Navigation Buttons */
        .buttons button {
            margin-left: 10px;
            padding: 12px 18px;
            font-size: 16px;
            cursor: pointer;
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            border: none;
            border-radius: 8px;
            transition: all 0.4s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        .buttons button:hover {
            transform: scale(1.1) rotate(3deg);
            background: linear-gradient(135deg, #217dbb, #1a5c8d);
            box-shadow: 0 8px 20px rgba(52, 152, 219, 0.4);
        }

        /* Content Sections */
        .section {
            min-height: 100vh;
            padding: 100px 20px 20px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transform: translateY(50px);
            transition: opacity 0.8s ease, transform 0.8s ease;
        }

        .section.visible {
            opacity: 1;
            transform: translateY(0);
        }

        .section h1 {
            font-size: 2.5em;
            margin-bottom: 30px;
            text-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        /* Image Gallery */
        .image-container {
            display: grid;
            gap: 25px;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            max-width: 1000px;
            width: 100%;
            padding: 20px;
        }

        .image-container img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-radius: 12px;
            opacity: 0;
            transform: translateY(30px) scale(0.95);
            transition: all 0.6s ease;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        .image-container img.visible {
            opacity: 1;
            transform: translateY(0) scale(1);
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 30px;
            background-color: rgba(30, 30, 30, 0.9);
            margin-top: 50px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        @media (max-width: 768px) {
            .top-bar {
                flex-direction: column;
                gap: 15px;
                padding: 15px;
            }

            .buttons {
                display: flex;
                flex-wrap: wrap;
                justify-content: center;
                gap: 10px;
            }

            .buttons button {
                margin-left: 0;
            }

            .section h1 {
                font-size: 2em;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <nav class="top-bar">
        <div class="search-bar">
            <input type="text" id="search" placeholder="Search...">
        </div>
        <div class="buttons">
            <button onclick="scrollToSection('biography')">Biography</button>
            <button onclick="scrollToSection('awards')">Awards</button>
            <button onclick="scrollToSection('history')">History</button>
            <button onclick="scrollToSection('initiatives')">Initiatives</button>
        </div>
    </nav>

    <section id="biography" class="section">
        <h1>Biography</h1>
        <p>Detailed story about the person's life, achievements, and contributions.</p>
    </section>

    <section id="awards" class="section">
        <h1>Awards</h1>
        <div class="image-container">
            <img src="award1.jpg" alt="Award 1" loading="lazy">
            <img src="award2.jpg" alt="Award 2" loading="lazy">
            <img src="award3.jpg" alt="Award 3" loading="lazy">
            <img src="award4.jpg" alt="Award 4" loading="lazy">
        </div>
    </section>

    <section id="history" class="section">
        <h1>History of Life</h1>
        <div class="image-container">
            <img src="history1.jpg" alt="Historical Moment 1" loading="lazy">
            <img src="history2.jpg" alt="Historical Moment 2" loading="lazy">
            <img src="history3.jpg" alt="Historical Moment 3" loading="lazy">
            <img src="history4.jpg" alt="Historical Moment 4" loading="lazy">
        </div>
    </section>

    <section id="initiatives" class="section">
        <h1>Initiatives</h1>
        <p>Engaging in various initiatives to improve technology and society.</p>
    </section>

    <footer class="footer">
        <p>Contact: example@gmail.com | Phone: +123 456 7890</p>
        <p>All Rights Reserved &copy; 2025</p>
    </footer>

    <script>
        // Scroll Animation Logic
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                    
                    // Stagger child image animations
                    const images = entry.target.querySelectorAll('img');
                    images.forEach((img, index) => {
                        img.style.transitionDelay = `${index * 0.15}s`;
                        img.classList.add('visible');
                    });
                }
            });
        }, { threshold: 0.1 });

        // Observe all sections
        document.querySelectorAll('.section').forEach(section => {
            observer.observe(section);
        });

        // Smooth Scroll Function
        function scrollToSection(sectionId) {
            const section = document.getElementById(sectionId);
            const headerHeight = document.querySelector('.top-bar').offsetHeight;
            const position = section.offsetTop - headerHeight;

            window.scrollTo({
                top: position,
                behavior: 'smooth'
            });
        }

        // Search Functionality
        document.getElementById('search').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                const query = e.target.value.toLowerCase();
                const sections = document.querySelectorAll('.section');
                
                sections.forEach(section => {
                    const content = section.textContent.toLowerCase();
                    section.style.display = content.includes(query) ? 'flex' : 'none';
                });
            }
        });

        // Responsive Image Handling
        window.addEventListener('resize', () => {
            document.querySelectorAll('.image-container img').forEach(img => {
                img.style.transitionDelay = '0s';
            });
        });
    </script>
</body>
</html>
